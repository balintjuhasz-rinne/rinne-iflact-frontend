import React from 'react';
import UnregisterScreen from '../layouts/unregister-screen';
import ResetPasswordForm from '../containers/forms/reset-password-form';

const ResetPassword = () => (
  <UnregisterScreen>
    <ResetPasswordForm />
  </UnregisterScreen>
);

export default ResetPassword;
