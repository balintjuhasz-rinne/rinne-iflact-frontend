import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import s from './sidebar-privacy.module.scss';

const SidebarPrivacy = () => (
  <div className={s.wrap}>
    s
  </div>
);

SidebarPrivacy.propTypes = {

};

export default memo(SidebarPrivacy);
