import { createSelector } from 'reselect';
import {
  INVITE_COSIGNATORY_MODAL,
  REMOVE_USER_MODAL,
  CREATE_COMPANY_MODAL,
  EDIT_COMPANY_MODAL,
  DELETE_COMPANY_MODAL,
  ERROR_MODAL,
  VOTING_MODAL,
  CREATE_RESOLUTION_MODAL,
  DELETE_RESOLUTION_MODAL,
  VOTED_COSIGNATORIES_MODAL,
  CANCEL_RESOLUTION_MODAL,
  DELETE_USER_MODAL,
  EDIT_USER_PASSWORD_MODAL,
  SUCCESS_MODAL,
  INVITE_COWORKER_MODAL,
  EDIT_RESOLUTION_MODAL,
  UPLOAD_TEMPLATE_MODAL,
  INACTIVATE_USER_MODAL,
} from '../constants/modal.constants';

export const getModalsSelector = createSelector(
  (state) => state.modals.get(INVITE_COSIGNATORY_MODAL),
  (state) => state.modals.get(REMOVE_USER_MODAL),
  (state) => state.modals.get(CREATE_COMPANY_MODAL),
  (state) => state.modals.get(EDIT_COMPANY_MODAL),
  (state) => state.modals.get(DELETE_COMPANY_MODAL),
  (state) => state.modals.get(ERROR_MODAL),
  (state) => state.modals.get(VOTING_MODAL),
  (state) => state.modals.get(CREATE_RESOLUTION_MODAL),
  (state) => state.modals.get(DELETE_RESOLUTION_MODAL),
  (state) => state.modals.get(EDIT_RESOLUTION_MODAL),
  (state) => state.modals.get(VOTED_COSIGNATORIES_MODAL),
  (state) => state.modals.get(CANCEL_RESOLUTION_MODAL),
  (state) => state.modals.get(DELETE_USER_MODAL),
  (state) => state.modals.get(EDIT_USER_PASSWORD_MODAL),
  (state) => state.modals.get(SUCCESS_MODAL),
  (state) => state.modals.get(INVITE_COWORKER_MODAL),
  (state) => state.modals.get(UPLOAD_TEMPLATE_MODAL),
  (state) => state.modals.get(INACTIVATE_USER_MODAL),
  (
    inviteCosignatoryModal,
    removeUserModal,
    createCompanyModal,
    editCompanyModal,
    deleteCompanyModal,
    errorModal,
    votingModal,
    createResolutionModal,
    deleteResolutionModal,
    editResolutionModal,
    votedCosignatoriesModal,
    cancelResolutionModal,
    deleteUserModal,
    editUserPasswordModal,
    successModal,
    inviteCoworkerModal,
    uploadTemplateModal,
    inactivateUserModal,
  ) => ({
    inviteCosignatoryModal,
    removeUserModal,
    createCompanyModal,
    editCompanyModal,
    deleteCompanyModal,
    errorModal,
    votingModal,
    createResolutionModal,
    deleteResolutionModal,
    editResolutionModal,
    votedCosignatoriesModal,
    cancelResolutionModal,
    deleteUserModal,
    editUserPasswordModal,
    successModal,
    inviteCoworkerModal,
    uploadTemplateModal,
    inactivateUserModal,
  }),
);
